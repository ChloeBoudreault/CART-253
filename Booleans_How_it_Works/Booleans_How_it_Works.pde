// a conditional statement
// if ( ) / else
// if (some test is true) then do something
// test must take the form of a Boolean result...
// test must evaluate to either TRUE or FALSE
// AND == &&
// OR == ||
// NOT = !
// the type to be used is Boolean

boolean trueArgument = true;
boolean falseArgument = false;

if (<test>) {
  <statements>
  } else if (<test>) {
  <statements>
} else {
  <statements>
}